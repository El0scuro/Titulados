version: '3.9'

networks:
  app-network:
    driver: bridge

services:
  db:
    image: mysql:8.1
    container_name: mysql-db
    environment:
      MYSQL_ROOT_PASSWORD: transporteSW0608
      MYSQL_DATABASE: tituladosv2
    ports:
      - "3310:3306"
    volumes:
      - db-data:/var/lib/mysql
      - ./tituladosv2.sql:/docker-entrypoint-initdb.d/tituladosv2.sql:ro
    networks:
      - app-network

  backend:
    build:
      context: ./back-end
    env_file:
      - ./back-end/.env
    ports:
      - "4300:4300"
    depends_on:
      - db
    networks:
      - app-network
    volumes:
      - ./back-end/src/archivos:/app/src/archivos
  frontend:
    build:
      context: ./front-end
    env_file:
      - ./front-end/.env
    ports:
      - "3000:3000"
    depends_on:
      - backend
    networks:
      - app-network

volumes:
  db-data: